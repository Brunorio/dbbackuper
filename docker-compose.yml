services:
  dbbackuper:
    build: 
      context: ./
    container_name: dbbackuper
    network_mode: "container:${DB_CONTAINER_NAME:-db}"
    volumes:
      - ${DOCKER_BACKUP_PATH}:/app/backups
      - ${DOCKER_LOG_PATH}:/app/logs


